backend:
  name: github
  repo: mazenswar/calm-v3
  branch: main
  base_url: https://calm-v3.vercel.app/api/decap
  auth_endpoint: oauth/authorize # IMPORTANT: no leading slash

media_folder: "public/uploads"
public_folder: "/uploads"

collections:
  - name: "blog"
    label: "Blog"
    folder: "content/blog"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - {
          label: "Author",
          name: "author",
          widget: "string",
          default: "Tanya Singh, PhD",
        }
      - { label: "Tags", name: "tags", widget: "list", default: ["therapy"] }
      - {
          label: "Featured Image",
          name: "image",
          widget: "image",
          required: false,
        }
      - { label: "Body", name: "body", widget: "markdown" }
